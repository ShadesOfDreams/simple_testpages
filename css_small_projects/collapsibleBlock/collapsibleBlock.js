HTMLElement.prototype.collapsibleBlock = function (options) {
  var collapsibleBlock = {
    title: "Collpasible"
  }

  // get innerHTML to place in the body
  var body = this.innerHTML;
  // add title block as header

}